class Solution:
    def isAnagram(self, s, t):
        #sort s and sort t, see if the sorted results are exactly the same
        sortedS=self.sort(s)
        sortedT=self.sort(t)
        if sortedS==sortedT:
            return True
        return False
    def selectionsort(self,string):
        arrayofbytes=list()
        for letter in string:
            
            
    def insertionsort(self,string):
        pass
    def shellsort(self,string):
        pass
                
        